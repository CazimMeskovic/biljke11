// Import necessary dependencies
import React from 'react';

// Create Welcome component
const Welcome = ({ registeredName }) => {
  return (
    <div>
      <h2>Welcome, {registeredName}!</h2>
      {/* Add other content or features you want to display */}
    </div>
  );
};

export default Welcome;
